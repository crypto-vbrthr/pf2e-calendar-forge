{
  "name": "pf2e-calendar-forge",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "find scripts tests -name '*.js' -print0 | xargs -0 -n1 node --check"
  }
}
